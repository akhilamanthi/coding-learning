package com.practice;
import java.util.Scanner;
public class prime {
public static void main(String args[]) {
	int input;
	primetest = new Int[];
    Scanner scanner = new Scanner(System.in);
    input=Sc.nextInt();
    for(Object i = 3;i<input;i++) {
    	boolean is boolean prime = is prime  number(i);
    	if(isprime==True) {
    		Object primelist;
			primelist.add(i);
    	}
    }
    boolean isPrimeNumber(int num) {
    	}
}
}
